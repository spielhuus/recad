[package]
name = "simulation"
version = "0.1.0"
edition = "2021"


[dependencies]
types = { path = "../types" }
models = { path = "../models" }
netlist = { path = "../netlist" }
ngspice = { path = "../ngspice" }

# uuid.workspace = true
# glam.workspace = true
spdlog-rs.workspace = true

